#include<stdio.h>
int fact(int);
int main()
{
int a;
printf("enter the value of a\n");
scanf("%d",&a);
printf("the factorial of %d is %d\n",a,fact(a));
return 0;
}
int fact(int n)
{
if (n==0||n==1)
    return 1;
else 
    return n*fact(n-1);
} 
