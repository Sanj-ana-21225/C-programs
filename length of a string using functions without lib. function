#include <stdio.h>
int stringLength(char str[]);  
int main()
{
    char str[100];
    int len;
    printf("Enter a string: ");
    gets(str);   
     len=stringlength(str);
    printf("Length of the string = %d\n", len);

    return 0;
}
int stringLength(char str[])
{
    int i;
    for(i=0; a[i]!='\0'; i++)
        i++;
    }

    return i;  
}
