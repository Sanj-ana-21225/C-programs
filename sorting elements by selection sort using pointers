#include <stdio.h>
int main() 
{
    int n, i, j, min, temp;
    printf("Enter number of elements: ");
    scanf("%d", &n);
    int arr[n];
    int *ptr = arr;
    printf("Enter %d elements:\n", n);
    for(i = 0; i < n; i++){
        scanf("%d", ptr + i);
     }
    for(i = 0; i < n - 1; i++) {
        min = i;
        for(j = i + 1; j < n; j++) {
            if(*(ptr + j) < *(ptr + min)
                min = j;
        }
      {
        temp = *(ptr + i);
        *(ptr + i) = *(ptr + min_index);
        *(ptr + min_index) = temp;
    }
    printf("Sorted array : ");
    for(i = 0; i < n; i++){
        printf("%d ", *(ptr + i));
    }  
   return 0;
}
